# Claude 桌面应用中文设置指南

## 📌 问题说明
你使用的是 **Claude 桌面应用程序**（GUI版本），而不是命令行CLI版本。这两种版本的设置方法是不同的！

## 🎯 解决方案（三种方法）

### ✅ 方法一：通过应用内设置（最推荐）

#### 步骤：

1. **点击左下角的个人资料图标**
   - 在你的截图左下角可以看到：`[ 我 ] 我就是先锋 · Free`
   - 点击这个图标

2. **找到语言设置**
   - 在弹出的菜单中找到 "Settings"（设置）
   - 然后找到 "Language"（语言）选项

3. **选择简体中文**
   - 点击语言下拉菜单
   - 选择 "Simplified Chinese"（简体中文）或 "zh-CN"

4. **保存并生效**
   - 语言会自动更新，界面立即变成中文！

---

### ✅ 方法二：修改配置文件（适用于设置找不到的情况）

#### Windows 用户：

1. **完全退出 Claude 应用**
   - 右键点击任务栏图标 → "退出"

2. **打开配置目录**
   - 路径：`C:\Users\[你的用户名]\AppData\Roaming\Claude\`
   - 可以按 `Win + R` 然后输入 `%AppData%\Claude` 直接打开

3. **编辑 config.json**
   - 用记事本打开 `config.json`
   - 找到或添加 `"language": "zh-CN"`
   - 完整示例：
```json
{
  "language": "zh-CN",
  "hasCompletedOnboarding": true,
  ...其他配置...
}
```

4. **保存并重启**
   - 保存文件，确保编码是 UTF-8
   - 重新启动 Claude 应用

#### macOS 用户：

1. **完全退出 Claude**
   - 菜单栏中选择 "Quit Claude"

2. **打开配置目录**
   - 路径：`~/Library/Application Support/Claude/`
   - 按 `Cmd + Shift + G` 输入路径

3. **编辑 config.json**
   - 添加 `"language": "zh-CN"`

---

### ✅ 方法三：重置应用重新初始化

如果以上方法都无效：

1. **完全退出 Claude**

2. **删除用户数据目录**
   - Windows：删除 `C:\Users\[用户名]\AppData\Roaming\Claude\` 整个文件夹
   - macOS：删除 `~/Library/Application Support/Claude/` 整个文件夹

3. **重新启动 Claude**
   - 首次启动时会弹出语言选择界面
   - 选择 "简体中文" 并确认

---

## 📊 方法对比

| 方法 | 难度 | 推荐度 | 适用场景 |
|------|------|--------|----------|
| 方法一：应用内设置 | ⭐ | ⭐⭐⭐⭐⭐ | 最简单，推荐首选 |
| 方法二：修改配置文件 | ⭐⭐ | ⭐⭐⭐⭐ | 设置菜单找不到时 |
| 方法三：重置初始化 | ⭐⭐ | ⭐⭐⭐ | 前两种方法都无效 |

---

## 🎨 预期效果

设置成功后，界面会变成：
- "Chat" → "聊天"
- "New chat" → "新建聊天"
- "Projects" → "项目"
- "Artifacts" → "产物"
- "Customize" → "自定义"
- "Free plan" → "免费版"
- "How can I help you today?" → "今天我能帮你做什么？"

---

## ⚠️ 注意事项

1. **先退出应用**：修改配置文件前一定要完全退出 Claude
2. **备份配置**：建议先备份原配置文件
3. **中文支持**：确保使用较新版本的 Claude（v2.0+）
4. **网络连接**：某些版本需要网络验证语言设置

---

## 🔍 验证设置是否生效

重启后，检查界面元素是否变成中文，或者输入测试问题：
"你好，现在是什么语言？"

如果回答是中文，说明设置成功！

---

## 💡 常见问题

**Q: 为什么我的界面还是英文？**
A: 可能需要先退出再重启，或者检查配置文件是否正确保存。

**Q: 哪里可以找到个人资料图标？**
A: 在应用左下角，显示你的用户名的位置。

**Q: 可以同时设置中文和英文吗？**
A: 界面只能选一种语言，但 Claude 可以用任意语言交流。

---

## 📚 相关文档

- [CLI 版本中文设置指南](./CLAUDE_CODE_中文配置指南.md)
- Claude 官方帮助文档
